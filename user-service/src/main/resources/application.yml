server:
  port: 8082

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql://localhost:3307/user_db
    username: root
    password: pass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true